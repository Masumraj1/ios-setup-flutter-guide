# iOS Setup & Folder Structure In-Depth Guide

এই রিপোজিটরিতে ফ্লাটার (Flutter) প্রজেক্টের iOS কনফিগারেশন এবং বিশেষ করে **CocoaPods (Pod)** এর কাজ সম্পর্কে বিস্তারিত আলোচনা করা হয়েছে।

## 📁 iOS Folder Structure (বিস্তারিত)

আইওএস ফোল্ডারের ভেতর গুরুত্বপূর্ণ ফাইলগুলোর কাজ নিচে দেওয়া হলো:

* **Runner.xcworkspace**: এটি আপনার মূল প্রজেক্ট ফাইল যা Xcode-এ ওপেন করতে হয়। এর মধ্যে আপনার কোড এবং সব লাইব্রেরি একসাথে থাকে।
* **Runner.xcodeproj**: এটি প্রজেক্টের মেইন কনফিগারেশন ফাইল।
* **Podfile**: এখানে আপনি বলে দেন আপনার অ্যাপে কোন কোন থার্ড-পার্টি লাইব্রেরি লাগবে এবং আপনার অ্যাপের সর্বনিম্ন iOS ভার্সন (যেমন: 14.0) কত হবে।
* **Pods/**: এই ফোল্ডারে CocoaPods এর মাধ্যমে ডাউনলোড করা সব লাইব্রেরি বা এক্সটার্নাল কোড জমা থাকে।
* **Runner/Info.plist**: এখানে অ্যাপের পারমিশন (যেমন: ক্যামেরা, লোকেশন) এবং অ্যাপের নাম সেট করা থাকে।

---

## 🛠 CocoaPods (Pod) কী এবং কেন?

**Pod** বা **CocoaPods** হলো iOS এর জন্য একটি **Dependency Manager**।

* **লাইব্রেরি ম্যানেজমেন্ট**: এটি আপনার অ্যাপের জন্য প্রয়োজনীয় বাইরের সব কোড বা প্যাকেজ অটোমেটিক ডাউনলোড করে।
* **ভার্সন কন্ট্রোল**: অ্যাপের জন্য কোন প্যাকেজের কোন ভার্সন সঠিক, সেটি এটি নিশ্চিত করে।
* **অটোমেটিক লিঙ্কিং**: এটি লাইব্রেরিগুলোকে আপনার প্রজেক্টের সাথে কানেক্ট করে যাতে রান করার সময় কোনো এরর না হয়।

---

## 🚀 প্রয়োজনীয় কমান্ডসমূহ (iOS Reset & Fix Guide)

যদি আপনার প্রজেক্টে `Podfile` খুঁজে না পাওয়া যায় বা বিল্ডে সমস্যা হয়, তবে নিচের ধাপগুলো অনুসরণ করুন:

### ১. অবস্থান ঠিক করা এবং ফাইল রিকভারি
যদি আপনি ভুল করে `ios` ফোল্ডারের ভেতর থেকে `cd ios` কমান্ড দেন এবং "No such file" এরর পান, তবে প্রথমে মেইন ডিরেক্টরিতে ফিরে আসতে হবে।

```bash
# মেইন প্রজেক্ট ফোল্ডারে ফিরে আসা
cd ..

# হারানো বা মিসিং iOS ফাইলগুলো (যেমন Podfile) পুনরায় তৈরি করা
flutter create


# ১. iOS ডিরেক্টরিতে প্রবেশ করুন
cd ios

# ২. পুরনো সব ক্যাশ এবং লক ফাইল মুছে ফেলুন
rm -rf Pods
rm Podfile.lock

# ৩. নতুন করে সব কনফিগারেশন এবং প্যাকেজ ইন্সটল করুন
pod install